# Linked List
 For using the Linked List files remove the packages Names and add `SinglyLinkedList.java` file and `Node.java` file to  your workspace.

- If by removing packages files showing errors then modify the packeges according to you(How you created folders). 